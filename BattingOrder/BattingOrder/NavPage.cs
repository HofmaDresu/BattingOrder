using Xamarin.Forms;

namespace BattingOrder
{
    class NavPage : NavigationPage
    {
        public NavPage(Page rootPage) : base(rootPage)
        {
            //BarBackgroundColor = Colors.Primary;
            //BarTextColor = Colors.LightTextColor;
            //BackgroundColor = Colors.Primary;
        }
    }
}
